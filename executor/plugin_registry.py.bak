import os

PLUGIN_DIR="plugins"

def list_plugins():

    if not os.path.exists(PLUGIN_DIR):
        return []

    result=[]

    for f in os.listdir(PLUGIN_DIR):

        if f.endswith(".py"):
            result.append(f[:-3])

    return result
